
struct Vector<ItemType> {
    data:[ItemType x 5];
}

func add(val1: i32, val2: i32) ~> i32 {
    return val1 + val2;
}

export func test(num: i32) ~> i32 {
    let my_vec:Vector<i32>;
    let my_num = 3 + 2;
    
    my_num = my_num + 2;

    libc_printf(("my_num is %i!\n":$c8), my_num);
    
    return (my_num * add(2, 8)) - 4 / 2;
}